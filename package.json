{
  "name": "cloudflare-cors-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "A CORS proxy running in a cloudfare worker",
  "main": "index.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "dev-remote": "wrangler dev --remote"
  },
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^3.59.0",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
